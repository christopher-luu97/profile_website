import { BlogSeries } from "./types";

// Named series/"suites" of posts. Add an entry here before writing posts
// that reference it via seriesId in posts.ts. A series with no posts yet
// simply won't render anywhere.
export const series: BlogSeries[] = [];
